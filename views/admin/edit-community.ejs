<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/main.css">

</head>

<body>
   <%- include('../includes/navigation.ejs') %>

    <main>
        <form class="com-form" action="/admin/<% if (editing) { %>edit-community<% } else { %>add-community<% } %>" method="POST">
            
            <div class="form-control">
                <label for="comName">Name</label>
                <input type="text" name="comName" id="comName" value="<% if (editing) { %><%= community.name %><% } %>">
            </div>

            <% if (editing) { %>
                <input type="hidden" value="<%= community.id %>" name="comId">
            <% } %>

            <button class="btn" type="submit"><% if (editing) { %>Verein aktualisieren<% } else { %>Verein hinzufügen<% } %></button>

        </form>
    </main>
<%- include('../includes/end.ejs') %>